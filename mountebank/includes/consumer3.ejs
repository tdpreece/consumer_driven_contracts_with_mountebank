{
    "predicates": [{
    "inject": "function(request) { return (request.method === \"POST\") && (request.path === \"/process-it\") && (request.body === \"{}\")}"
    }],
    "responses": [{
      "is": {
        "statusCode": 200,
        "headers": {
          "Location": "http://localhost:4545/customers/123",
          "Content-Type": "application/json"
        },
        "body": "{\"id\": 123}"
      }
    }]
}
