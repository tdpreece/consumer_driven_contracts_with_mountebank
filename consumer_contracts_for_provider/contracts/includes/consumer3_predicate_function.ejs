function (request, logger) {
    if (request.path !== "/record") {
        return false;
    }
    if (request.method !== "POST") {
        return false;
    }

    var message = JSON.parse(request.body);
    var expected_message = {"a": 123, "b": 222, "c": 333}
    if (JSON.stringify(message) !==  JSON.stringify(expected_message)) {
        return false;
    }

    return true;
}
